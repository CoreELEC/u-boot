if TARGET_S7D_SKT

config SYS_SOC
	default "s7d"

config SYS_BOARD
	default "s7d_skt"

config SYS_CONFIG_NAME
	default "s7d_skt"

config DTB_BIND_KERNEL
    bool "load dtb from boot part but not flash rsv"
    default n
config BL30_SELECT
	string "s7d BL30 board config"
		default "s7d_pxp"
config CMD_PLLTEST
        bool "support plltest command"
        default n
		help
			support plltest command

config AML_REBOOT
		bool "reboot cmd"
		default y
		help
			support uboot reboot cmd
endif
